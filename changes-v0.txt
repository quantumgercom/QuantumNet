Commit: fix(qubit): corrige simulação da Hadamard e renomeia get_last_qubit {
1.1 - apply_hadamard() agora usa marcadores de superposicao (2 para |+>, 3 para |->) em vez de random.choice direto. Isso garante que H(H|psi>) = |psi> (aplicar Hadamard duas vezes retorna ao estado original). O colapso aleatorio agora ocorre apenas no measure(), quando o qubit esta em superposicao. Alteracao em qubit.py.

1.2 - get_last_qubit() renomeado para consume_last_qubit() para refletir o efeito colateral destrutivo (remocao do qubit da memoria). Internamente, self.memory.remove(q) (busca linear O(n)) foi substituido por self.memory.pop() (O(1)). Todas as referencias em physical_layer.py foram atualizadas. Alteracao em host.py e physical_layer.py.
}

Commit: fix(network): renomeia topologias para ingles e adiciona erro para nome invalido em set_ready_topology{
    1.3 - Nomes das topologias em set_ready_topology() renomeados de portugues para ingles ('Grade' -> 'Grid', 'Linha' -> 'Line', 'Anel' -> 'Ring'), alinhando implementacao com a documentacao existente. Adicionado bloco else com raise ValueError para topologias desconhecidas, eliminando falha silenciosa. Alteracao em network.py.
}

Commit: fix(network): preserva referencia do grafo ao atualizar topologia em set_ready_topology{
    1.4 - set_ready_topology() agora atualiza o grafo in-place com clear()+update() em vez de reatribuir self._graph a um novo objeto. Isso preserva a referencia original compartilhada via NetworkContext, eliminando o risco de camadas ou contexto ficarem com referencia obsoleta ao grafo antigo. Removida a reatribuicao manual self._context.graph = self._graph, que se tornou desnecessaria. Alteracao em network.py.
}

Commit: fix(controller): implementa validacao real em check_route{
    1.5 - check_route() agora valida se a rota e valida no grafo da rede: verifica se a lista nao esta vazia, se todos os nos existem no grafo e se ha arestas conectando os nos consecutivos. Isso substitui o return True incondicional que mascarava bugs ao aceitar qualquer rota. Alteracao em controller.py.
}

Commit: fix(qubit): centraliza geracao de IDs de qubit para evitar colisoes{
    1.6 - Criado gerador centralizado de IDs de qubit em NetworkContext (metodo generate_qubit_id() com contador _next_qubit_id). ApplicationLayer.prepare_e91_qubits() agora usa o gerador centralizado em vez de random.randint(0, 1000), eliminando colisoes de IDs. PhysicalLayer.create_qubit() tambem migrado para usar o gerador centralizado, removendo o contador _count_qubit local. Isso garante unicidade global de IDs entre todas as camadas, evitando corrupcao do qubit_timeslots. Alteracoes em network_context.py, application_layer.py e physical_layer.py.
}

Commit: fix(epr): corrige consistencia de decoerencia e nomenclatura em EPR{
    1.8 - Problema ja resolvido pela refatoracao anterior que substituiu o callback _decoherence_on_tick() por avaliacao lazy em get_current_fidelity(). Tanto Qubit quanto Epr agora calculam decoerencia sob demanda com a formula base_fidelity * (decoherence_rate ** elapsed), onde elapsed = clock.now - base_timeslot. A guarda elapsed <= 0 impede aplicacao de decoerencia no mesmo timeslot de criacao, eliminando a inconsistencia original. Renomeado set_fidelity() para set_current_fidelity() em Epr para alinhar com Qubit. Alteracao em epr.py.
}

Commit: fix(link): corrige contagem de falhas acumulativa global em LinkLayer.request{
    1.9 - self._failed_requests (lista global) substituida por contador self._failed_request_count (para metricas/log) e contador local failures passado por parametro em _start_attempt(). A decisao de purificacao agora usa o contador local da requisicao corrente, nao o acumulado global. Falhas sao registradas via log e incremento do contador global, sem armazenar tuplas em lista. Propriedade renomeada de failed_requests para failed_request_count. Notebook link_layer.ipynb atualizado. Alteracoes em link_layer.py e link_layer.ipynb.
}

Commit: fix(imports): padroniza imports para relativos em todas as camadas{
    2.1 - Imports absolutos (from quantumnet.utils, from quantumnet.topology, from quantumnet.quantum) substituidos por imports relativos (from ..utils, from ..topology, from ..quantum) em link_layer.py, network_layer.py, transport_layer.py e application_layer.py. Isso elimina a inconsistencia com physical_layer.py, que ja usava imports relativos, e garante que o pacote funcione independentemente do nome com que for importado. Alteracoes em link_layer.py, network_layer.py, transport_layer.py e application_layer.py.
}

Commit: fix(qubit,epr): converte getters/setters Java-style para @property{
    2.2 - Metodos get_current_fidelity(), set_current_fidelity() e get_initial_fidelity() substituidos por @property current_fidelity (com setter) e @property initial_fidelity em Qubit e Epr, seguindo o estilo idiomatico Python ja adotado em Host. Todos os callers atualizados: qubit.get_current_fidelity() -> qubit.current_fidelity, qubit.set_current_fidelity(v) -> qubit.current_fidelity = v, qubit.get_initial_fidelity() -> qubit.initial_fidelity, e equivalentes em Epr. Alteracoes em qubit.py, epr.py, physical_layer.py, link_layer.py, network_layer.py e transport_layer.py.
}

Commit: refactor(layers): substitui contadores used_eprs/used_qubits por logging{
    2.5 - Atributos used_eprs e used_qubits (contadores inteiros) e metodos get_used_eprs()/get_used_qubits() removidos de todas as camadas (Physical, Link, Network, Transport, Application), eliminando codigo 100% duplicado. Cada self.used_eprs += N e self.used_qubits += N foi substituido por self.logger.log() com a mesma informacao de uso. Removidos tambem get_total_used_eprs() e get_total_used_qubits() de network.py e as entradas "Used EPRs"/"Used Qubits" de get_metrics(). Notebooks atualizados para remover chamadas a esses metodos. Alteracoes em physical_layer.py, link_layer.py, network_layer.py, transport_layer.py, application_layer.py, network.py e todos os notebooks de documentacao.
}

Commit: fix(exceptions): substitui Exception generica por excecoes customizadas{
    2.8 - Criado modulo quantumnet/exceptions.py com hierarquia de excecoes customizadas: QuantumNetError (base), SingletonError, HostNotFoundError, DuplicateHostError e TopologyError. Cada raise Exception(...) foi substituido pela excecao semantica correspondente: SingletonError em logger.py, TypeError (builtin) para validacao de tipo em host.py, HostNotFoundError em physical_layer.py, DuplicateHostError e TopologyError em network.py. Todas as excecoes customizadas herdam de QuantumNetError, permitindo captura granular com except especifico ou generica com except QuantumNetError. Excecoes exportadas no __init__.py do pacote. Alteracoes em exceptions.py (novo), logger.py, host.py, physical_layer.py, network.py e __init__.py.
}

Commit: fix(exceptions): remove excecoes que interrompiam simulacao em situacoes normais da rede{
    2.8 - Removidas HostMemoryError e EprNotFoundError, que interrompiam a simulacao em situacoes normais da rede (host sem qubits, canal sem EPRs). consume_last_qubit() em host.py agora retorna None em vez de lancar HostMemoryError. Os tres callers em physical_layer.py (_do_heralding, _do_on_demand, _do_on_replay) agora verificam se algum qubit retornou None e, nesse caso, logam a falha e invocam on_complete(success=False) sem interromper a simulacao. remove_epr() em network.py agora retorna None em vez de lancar EprNotFoundError. Classes HostMemoryError e EprNotFoundError removidas de exceptions.py e __init__.py. Alteracoes em host.py, physical_layer.py, network.py, exceptions.py e __init__.py.
}

Commit: refactor(layers): substitui lista created_eprs por acumuladores de fidelidade{
    3.3 - Listas created_eprs em PhysicalLayer, LinkLayer e TransportLayer removidas. O padrao fragil de transferencia manual (extend + clear em 3 lugares da LinkLayer) foi eliminado. Em seu lugar, _do_heralding() em physical_layer.py agora propaga epr_fidelity no callback on_complete(success, epr_fidelity). A LinkLayer acumula soma e contagem de fidelidade em _epr_fidelity_sum e _epr_count diretamente no callback on_heralding_done, capturando o valor no momento da criacao. avg_fidelity_on_linklayer() passa a usar os acumuladores em vez da lista de objetos. Em transport_layer.py, created_eprs era codigo morto (nunca populado nem lido) e foi removido. Alteracoes em physical_layer.py, link_layer.py e transport_layer.py.
}

Commit: refactor(physical): unifica echp_on_demand e echp_on_replay em metodo echp(mode){
    3.1 - echp_on_demand() e echp_on_replay() removidos. Substituidos por echp(alice_host_id, bob_host_id, mode) e _do_echp(). mode='on_demand' usa costs.on_demand e prob_on_demand_epr_create; mode='on_replay' usa costs.replay e prob_replay_epr_create. Eventos emitidos preservados (echp_on_demand_success/failed, echp_on_replay_success/failed) via f-string com o mode. Notebook physical_layer.ipynb e docs/clock_ticks.md atualizados. Alteracao em physical_layer.py.
}

Commit: fix(typing): corrige type hints e verificacoes de tipo em link_layer, host e epr{
    4.1 - purification_calculator em link_layer.py: parametros f1 e f2 corrigidos de int para float, pois representam fidelidades (valores entre 0 e 1). Docstring atualizada.
    4.2 - Host.add_connection em host.py: substituido type(x) != int por not isinstance(x, int), aceitando subclasses de int como numpy.int64 usadas pelo NetworkX.
    4.3 - Host.info() em host.py: comparacao self.routing_table == None substituida por self.routing_table is None, seguindo a convencao PEP 8.
    4.4 - Epr.__init__ em epr.py: epr_id anotado como Union[int, tuple] para refletir os dois usos reais: int em physical_layer e tuple (alice_id, bob_id) em link_layer e network_layer.
}

Commit: fix(style): corrige convencoes de nomenclatura e adiciona repr/eq/hash em Qubit e Epr{
    6.4 - Nao aplicavel: set_current_fidelity() ja removido na correcao 2.2 (substituido por @property setter). Nenhuma alteracao necessaria.
    6.5 - Parametros Alice e Bob em network_layer.py renomeados para alice e bob (snake_case), nas assinaturas e docstrings de short_route_valid() e entanglement_swapping(). Alteracao em network_layer.py.
    6.6 - __repr__ adicionado a Qubit e Epr, expondo id, estado(qubit) e fidelidade atual com 4 casas decimais. Alteracao em qubit.py e epr.py.
    6.7 - __eq__ e __hash__ adicionados a Qubit e Epr com base no respectivo id, tornando list.remove() e operacoes de colecao dependentes de valor em vez de identidade de objeto. Alteracao em qubit.py e epr.py.
}

Commit: refactor(logger): remove globais e reimplementa ativacao com handlers nativos{
    - Removidos FORMAT global e logging.basicConfig(), que poluiam a configuracao de logging do processo inteiro. Removido atributo de classe DISABLED. __init__ agora limpa handlers herdados, adiciona NullHandler e seta nivel base DEBUG, garantindo saida silenciosa por default. activate() reimplementado com parametros level (default 'INFO'), console (bool), file_log (bool) e filename (str opcional); gera nome com timestamp se filename nao fornecido; configura FileHandler e/ou StreamHandler conforme os flags. Metodos warn/error/log/debug simplificados para delegar diretamente a self.logger, sem guarda DISABLED. Adicionado import datetime. Alteracao em logger.py.
}

Commit: refactor(metrics): reimplementa coleta de metricas com MetricsCollector orientado a eventos{
    1 - Criado MetricsCollector em quantumnet/utils/metrics.py. O arquivo CSV e aberto uma unica vez no __init__ e mantido aberto durante toda a simulacao. Fechamento seguro via close() (flush + close) com suporte a context manager (__enter__/__exit__). Export adicionado em utils/__init__.py. Alteracoes em metrics.py (novo) e utils/__init__.py.
    2 - Schema generico do CSV com colunas fixas: clock_tick, event_type, source_node, target_node, value, details. source_node extraido por prioridade de chaves (alice, host_id, node); target_node de (bob, target); value de (fidelity, key_length, count, route_len, etc.); demais primitivos vao para details como JSON compacto. Alteracao em metrics.py.
    3 - MetricsCollector nunca armazena referencias de objetos: so primitivos (int, float, str, bool, None) e tuples/listas convertidas para string passam pelo filtro _safe_primitive(). Removido transmitted_qubits em TransportLayer (que retinha qubit_alice, a causa do memory leak). Removido _requests em LinkLayer. Removido routes_used em NetworkLayer. Removidos _qubit_timeslots e register_qubit_creation() em NetworkContext e PhysicalLayer. Clock ganhou listen_all(callback) para registro de ouvintes wildcard com assinatura callback(clock, event_name, **data), disparados ao final de emit() sem alterar o timeslot. Alteracoes em clock.py, network_context.py, physical_layer.py, metrics.py.
    4 - Cada evento e convertido e escrito imediatamente como uma linha CSV dentro de _record(); nenhum buffer cresce em memoria. O flush no SO e delegado ao sistema operacional (sem flush() por linha). Removidos get_metrics() em Network, avg_fidelity_on_linklayer() e _epr_fidelity_sum/_epr_count em LinkLayer, avg_fidelity_on_transportlayer()/get_teleported_qubits() em TransportLayer, get_avg_size_routes()/avg_size_routes em NetworkLayer, qubit_timeslots/register_qubit_creation()/display_all_qubit_timeslots() em Network. Imports os e csv removidos de network.py. Alteracoes em link_layer.py, transport_layer.py, network_layer.py, network.py.
}
