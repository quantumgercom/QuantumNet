Commit: fix(qubit): corrige simulação da Hadamard e renomeia get_last_qubit {
1.1 - apply_hadamard() agora usa marcadores de superposicao (2 para |+>, 3 para |->) em vez de random.choice direto. Isso garante que H(H|psi>) = |psi> (aplicar Hadamard duas vezes retorna ao estado original). O colapso aleatorio agora ocorre apenas no measure(), quando o qubit esta em superposicao. Alteracao em qubit.py.

1.2 - get_last_qubit() renomeado para consume_last_qubit() para refletir o efeito colateral destrutivo (remocao do qubit da memoria). Internamente, self.memory.remove(q) (busca linear O(n)) foi substituido por self.memory.pop() (O(1)). Todas as referencias em physical_layer.py foram atualizadas. Alteracao em host.py e physical_layer.py.
}

Commit: fix(network): renomeia topologias para ingles e adiciona erro para nome invalido em set_ready_topology{
    1.3 - Nomes das topologias em set_ready_topology() renomeados de portugues para ingles ('Grade' -> 'Grid', 'Linha' -> 'Line', 'Anel' -> 'Ring'), alinhando implementacao com a documentacao existente. Adicionado bloco else com raise ValueError para topologias desconhecidas, eliminando falha silenciosa. Alteracao em network.py.
}

Commit: fix(network): preserva referencia do grafo ao atualizar topologia em set_ready_topology{
    1.4 - set_ready_topology() agora atualiza o grafo in-place com clear()+update() em vez de reatribuir self._graph a um novo objeto. Isso preserva a referencia original compartilhada via NetworkContext, eliminando o risco de camadas ou contexto ficarem com referencia obsoleta ao grafo antigo. Removida a reatribuicao manual self._context.graph = self._graph, que se tornou desnecessaria. Alteracao em network.py.
}

Commit: fix(controller): implementa validacao real em check_route{
    1.5 - check_route() agora valida se a rota e valida no grafo da rede: verifica se a lista nao esta vazia, se todos os nos existem no grafo e se ha arestas conectando os nos consecutivos. Isso substitui o return True incondicional que mascarava bugs ao aceitar qualquer rota. Alteracao em controller.py.
}

Commit: fix(qubit): centraliza geracao de IDs de qubit para evitar colisoes{
    1.6 - Criado gerador centralizado de IDs de qubit em NetworkContext (metodo generate_qubit_id() com contador _next_qubit_id). ApplicationLayer.prepare_e91_qubits() agora usa o gerador centralizado em vez de random.randint(0, 1000), eliminando colisoes de IDs. PhysicalLayer.create_qubit() tambem migrado para usar o gerador centralizado, removendo o contador _count_qubit local. Isso garante unicidade global de IDs entre todas as camadas, evitando corrupcao do qubit_timeslots. Alteracoes em network_context.py, application_layer.py e physical_layer.py.
}

Commit: fix(epr): corrige consistencia de decoerencia e nomenclatura em EPR{
    1.8 - Problema ja resolvido pela refatoracao anterior que substituiu o callback _decoherence_on_tick() por avaliacao lazy em get_current_fidelity(). Tanto Qubit quanto Epr agora calculam decoerencia sob demanda com a formula base_fidelity * (decoherence_rate ** elapsed), onde elapsed = clock.now - base_timeslot. A guarda elapsed <= 0 impede aplicacao de decoerencia no mesmo timeslot de criacao, eliminando a inconsistencia original. Renomeado set_fidelity() para set_current_fidelity() em Epr para alinhar com Qubit. Alteracao em epr.py.
}
